# Modulos e pacotes
# Crie __init__.py dentro duma pasta como inicializar um pacote, o mesmo em sub-pastas
# Importe o pacote com o comando abaixo.
from utilidadesCeV import numeros
print(numeros.fatorial(5), numeros.dobro(5), numeros.tripo(10))
